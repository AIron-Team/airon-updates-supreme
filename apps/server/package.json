{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "fastify": "^5.3.3",
    "@fastify/cors": "^11.0.1",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@airon-updates-supreme/auth": "workspace:*",
    "@airon-updates-supreme/db": "workspace:*"
  },
  "trustedDependencies": [
    "supabase"
  ],
  "devDependencies": {
    "typescript": "^5.8.2",
    "tsx": "^4.19.2",
    "@types/node": "^22.13.11",
    "tsdown": "^0.16.5",
    "@airon-updates-supreme/config": "workspace:*"
  }
}
